proc* called_from_c(memory: s32*) -> s32 {
    ret fish_problem(100.0, 50.0, 10.0);
}

proc fish_problem(a: float32, b: float32, c: float32) -> s32 {
    i : s32 = 0;

    repeat {
        if a - b < c break;
        i = i + 1;
        a = a + a*0.15 - b;
    }

    ret i;
}


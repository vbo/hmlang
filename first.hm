proc* main_i32_i32_reti32(a: i32, b: i32) -> i32 {

    tmp: vec2;

    vec2_init(&tmp, a, b);

    eq : bool = tmp.x == tmp.y;
    eq1 := eq;

    ret tmp.x + tmp.y;

    type vec2 {
        x: i32;
        y: i32;
    }

    proc vec2_init(this: vec2*, x: i32, y: i32) -> void {
        this.x = x;
        this.y = y;
        ret void;
    }

    proc vec2_get_x_ptr(this: vec2*) -> i32* {
        ret &this.x;
    }

    proc vec2_identp(this: vec2*) -> vec2* {
        ret this;
    }
}

